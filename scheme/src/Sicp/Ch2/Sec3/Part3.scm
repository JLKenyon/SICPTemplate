#lang scheme

(provide sicp-main-ch2-sec3-part3)

(define (sicp-main-ch2-sec3-part3)
    (display "********************************************************************************\n")
    (display "sicp-main-ch2-sec3-part3\n")
)

(define (create-set values) #f)
(define (element-of-set? x set) #f)
(define (adjoin-set x set) #f)
(define (intersection-set set1 set2) #f)

; 2.59
(define (union-set set1 set2) #f)


; 2.60
(define (create-repset values) #f)
(define (element-of-repset? x set) #f)
(define (adjoin-repset x set) #f)
(define (intersection-repset set1 set2) #f)
