#lang scheme

(provide sicp-main-ch2-sec4-part1
         rect:add-complex
         rect:sub-complex
         rect:mul-complex
         rect:div-complex
         rect:real-part
         rect:imag-part
         rect:magnitude
         rect:angle    
         rect:make-from-real-imag
         rect:make-from-mag-ang
         polar:add-complex
         polar:sub-complex
         polar:mul-complex
         polar:div-complex
         polar:real-part  
         polar:imag-part  
         polar:magnitude  
         polar:angle      
         polar:make-from-real-imag
         polar:make-from-mag-ang  
)

(define (sicp-main-ch2-sec4-part1)
    (display "********************************************************************************\n")
    (display "sicp-main-ch2-sec4-part1\n")
)

; Representations for Complex Numbers

(define (rect:add-complex z1 z2)       #f)
(define (rect:sub-complex z1 z2)       #f)
(define (rect:mul-complex z1 z2)       #f)
(define (rect:div-complex z1 z2)       #f)
(define (rect:real-part   z)           #f)
(define (rect:imag-part   z)           #f)
(define (rect:magnitude   z)           #f)
(define (rect:angle       z)           #f)
(define (rect:make-from-real-imag x y) #f)
(define (rect:make-from-mag-ang   r a) #f)

(define (polar:add-complex z1 z2)       #f)
(define (polar:sub-complex z1 z2)       #f)
(define (polar:mul-complex z1 z2)       #f)
(define (polar:div-complex z1 z2)       #f)
(define (polar:real-part   z)           #f)
(define (polar:imag-part   z)           #f)
(define (polar:magnitude   z)           #f)
(define (polar:angle       z)           #f)
(define (polar:make-from-real-imag x y) #f)
(define (polar:make-from-mag-ang   r a) #f)

